# pc: AMD 9800X3D / RTX 5080 desktop, binhost server for the fleet.
# Plain sh vars, sourced by provision.sh and the numbered scripts.
# shellcheck disable=SC2034
BINHOST_ROLE=server
WANT_VIRT=1
WANT_OLLAMA=1
EXPECT_GPU=nvidia
EXPECT_CHASSIS=desktop
