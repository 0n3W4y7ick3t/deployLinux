# AUR-only packages. bootstrap.sh does not install these (yay refuses to
# run as root); it prints the yay command to run as your user.
nsxiv
google-cloud-cli
# cursor theme, macOS (+ macOS-White); Gentoo gets it from the @hyprland set
apple_cursor
# terminal mail configurator (stack itself is in pkgs-desktop.txt)
mutt-wizard
